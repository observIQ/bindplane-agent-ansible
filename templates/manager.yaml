# Managed by Ansible. Changes to this file will result in
# reconfiguration when Ansible is executed.
#
endpoint: "{{ endpoint }}"
secret_key: "{{ secret_key }}"
agent_id: "{{ agent_id }}"
labels: "{{ labels }}"
tls_config:
  cert_file: "{{ tlscert }}"
  key_file: "{{ tlskey }}"
  ca_file: "{{ cacert }}"
  insecure_skip_verify: {{ insecure_skip_verify }}
